import { Persona, Organizacion, Contacto, Recurso } from '../types';

/**
 * No real Personas/Organizaciones/Contactos exist anywhere in the app
 * today (they were never modeled), so there is nothing to reconcile —
 * left empty for the admin to fill in, same criterion as Bonos in Sprint 2.
 */
export const PERSONA_SEED: Persona[] = [];
export const ORGANIZACION_SEED: Organizacion[] = [];
export const CONTACTO_SEED: Contacto[] = [];

/** Recurso had no real data in Sprint 2 either (the tab launched empty). */
export const RECURSO_SEED: Recurso[] = [];
